# Anyon Remote Server 환경 변수 설정
# 이 파일을 .env.remote로 복사하고 값을 채워주세요

# ========================================
# JWT 비밀키 (아래 명령어로 생성)
# openssl rand -base64 48
# ========================================
VIBEKANBAN_REMOTE_JWT_SECRET=YOUR_JWT_SECRET_HERE

# ========================================
# 데이터베이스 연결
# ========================================
# RDS PostgreSQL을 사용하는 경우:
# SERVER_DATABASE_URL=postgresql://postgres:PASSWORD@your-rds-endpoint.rds.amazonaws.com:5432/vibekanban

# 또는 Docker Compose의 로컬 PostgreSQL 사용:
SERVER_DATABASE_URL=postgresql://postgres:postgres@db:5432/vibekanban

# ========================================
# 서버 설정
# ========================================
SERVER_LISTEN_ADDR=0.0.0.0:8081
SERVER_ACTIVITY_CHANNEL=activity
SERVER_ACTIVITY_BROADCAST_SHARDS=16
SERVER_ACTIVITY_BROADCAST_CAPACITY=512

# ========================================
# 공개 URL (서버 IP 또는 도메인)
# ========================================
SERVER_PUBLIC_BASE_URL=http://43.200.12.99:3000

# ========================================
# GitHub OAuth (필수)
# https://github.com/settings/developers 에서 생성
# ========================================
GITHUB_OAUTH_CLIENT_ID=your_github_client_id
GITHUB_OAUTH_CLIENT_SECRET=your_github_client_secret

# Authorization callback URL 설정:
# http://43.200.12.99:3000/oauth/callback

# ========================================
# Google OAuth (선택사항)
# ========================================
# GOOGLE_OAUTH_CLIENT_ID=your_google_client_id
# GOOGLE_OAUTH_CLIENT_SECRET=your_google_client_secret

# ========================================
# 이메일 서비스 (선택사항)
# Loops.so 무료 계정: https://loops.so
# ========================================
LOOPS_EMAIL_API_KEY=dummy_key_for_now

# ========================================
# Vite 프론트엔드 설정
# ========================================
VITE_APP_BASE_URL=http://43.200.12.99:3000
VITE_API_BASE_URL=http://43.200.12.99:3000
